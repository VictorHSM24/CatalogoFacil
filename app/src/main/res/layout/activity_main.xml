<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="32dp"
    android:gravity="center"
    android:background="@color/fundo_base"
    tools:context=".ui.MainActivity">

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Catalogo Fácil"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="@color/texto_icones"
        android:layout_marginBottom="64dp"/>

    <Button
        android:id="@+id/buttonCadastrarPedido"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cadastrar Novo Pedido"
        android:textSize="18sp"
        android:padding="16dp"
        android:backgroundTint="@color/primaria"
        android:textColor="@color/white"
        android:layout_marginBottom="24dp"/>

    <Button
        android:id="@+id/buttonVisualizarPedidos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Visualizar Todos os Pedidos"
        android:textSize="18sp"
        android:padding="16dp"
        android:backgroundTint="@color/primaria"
        android:textColor="@color/white"/>

</LinearLayout>